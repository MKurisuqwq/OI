#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using pii = pair<int, int>;

const int MAXN = 1e5 + 10;
int n, m, q;

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    int c;
    cin >> c >> n >> m >> q;
    for (int i = 1; i <= q; i++) {
        ;
    }

    return 0;
}